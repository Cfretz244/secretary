[project]
name = "companion"
version = "1.0.0"
description = "macOS Messages companion server for iOS Secretary"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
